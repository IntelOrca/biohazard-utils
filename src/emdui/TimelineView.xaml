<UserControl x:Class="emdui.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:emdui"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Focusable="True"
             PreviewKeyDown="canvas_PreviewKeyDown">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="DarkGray">
            <ToolBarTray HorizontalAlignment="Right" Background="Transparent" IsLocked="True">
                <ToolBar x:Name="toolbar" Background="Transparent" OverflowMode="Never" Padding="0,0,32,0" Margin="0,0,-20,0" />
            </ToolBarTray>
        </Border>
        <Border Padding="32,0,0,0">
            <ScrollViewer Background="White" HorizontalScrollBarVisibility="Visible">
                <Canvas x:Name="canvas"
                        Background="White"
                        Width="1100"
                        Height="1000"
                        PreviewMouseDown="canvas_PreviewMouseDown"
                        PreviewMouseMove="canvas_PreviewMouseMove" />
            </ScrollViewer>
        </Border>
    </DockPanel>
</UserControl>
